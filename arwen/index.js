'use strict';

(function() {
	angular.module("arwen", ["ngRoute", "ngCookies"])
		.config(config);
	
	config.$inject = ["$routeProvider"];

	function config($routeProvider) {
		$routeProvider
			
	}
})();
